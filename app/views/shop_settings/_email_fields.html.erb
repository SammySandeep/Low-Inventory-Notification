<tr class="nested-fields">
  <td>
    <%= link_to_remove_association "remove email", f, class: 'btn btn-primary btn-xs' %>
  </td>
  <td><%= f.text_field :email, label: false %></td>
  <td><%= f.check_box :is_admin, label: false %></td>
  <td><%= f.check_box :is_active, label: false %></td>
</tr>